-- =============================================================
-- 0010_finance_category.sql — 금융/핀테크 카테고리 추가
-- =============================================================
-- 실행 방법: Supabase Dashboard > SQL Editor 에서 전체를 붙여넣어 실행
-- 멱등: ON CONFLICT DO NOTHING (이미 존재하면 무시)
-- =============================================================

INSERT INTO public.categories (slug, label_ko, label_en, emoji, sort_order)
VALUES ('finance', '금융/핀테크', 'Finance', '💸', 7)
ON CONFLICT (slug) DO NOTHING;

-- =============================================================
-- 완료
-- =============================================================
-- 적용 후:
-- - 앱 등록/수정 폼의 카테고리 칩에 '금융/핀테크'가 자동으로 표시됩니다.
--   (UI는 DB categories를 동적으로 읽으므로 별도 코드 수정 불필요)
-- - data/seed.ts의 CATEGORIES 배열도 동기 추가됐습니다.
-- =============================================================
